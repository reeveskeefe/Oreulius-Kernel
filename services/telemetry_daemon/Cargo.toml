[package]
name = "telemetry_daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
# Using a fixed-point or standard math library suited for userspace.
# In userspace, we have the luxury of using std and float logic safely.
